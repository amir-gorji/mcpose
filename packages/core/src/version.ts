/**
 * The mcpose library version, inlined so it survives bundling (a
 * `createRequire('../package.json')` lookup breaks when consumers bundle
 * the package). Kept in sync with package.json by `version.test.ts`.
 */
export const VERSION = '2.2.0';
